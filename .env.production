# Production build configuration.
#
# Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY as Project Environment
# Variables in Vercel (Production + Preview). Vercel injects them at build time
# and they take precedence over .env files, so nothing secret is committed here.
